publish_mode: editorial_workflow  # ✅ Active les drafts (brouillons) + PR

backend:
  name: github
  repo: RegistreSecurite/support_deemply
  branch: main
  base_url: https://support.deemply.com  # ✅ Ton serveur PHP d’OAuth
  auth_endpoint: "admin-oauth/public/auth"  # ✅ Important, évite /auth/auth

media_folder: docs/public/images  # ✅ Là où les images sont stockées dans le repo
public_folder: /images  # ✅ Là où elles seront visibles dans le site généré

collections:
  - name: 'guide'
    label: 'Guide'
    folder: 'docs/guide'
    create: true
    slug: '{{slug}}'
    editor:
      preview: true  # ✅ Active l’aperçu Markdown dans le CMS
    nested:
      depth: 10  # Profondeur maximale des sous-dossiers
      summary: '{{title}}'
    path: '{{folder}}/{{slug}}'
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Description', name: 'description', widget: 'string' }
      - { 
          label: 'Dossier', 
          name: 'folder', 
          widget: 'select',
          options: [
            { label: 'Fonctionnalités communes - Activités', value: 'fonctionnalités communes/activities' },
            { label: 'Fonctionnalités communes - Interventions', value: 'fonctionnalités communes/interventions' },
            { label: 'Fonctionnalités communes - Matériels', value: 'fonctionnalités communes/materials' },
            { label: 'Fonctionnalités communes - Autres', value: 'fonctionnalités communes/others' },
            { label: 'Fonctionnalités communes - Fournisseurs', value: 'fonctionnalités communes/providers' },
            { label: 'Fonctionnalités communes - Sites', value: 'fonctionnalités communes/sites' },
          ]
        }
      - { label: 'Body', name: 'body', widget: 'markdown' }